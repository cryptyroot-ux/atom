//! atom-cli: atom CLI: setup/mission/grant/approval/effect/... (Blueprint §17).
//! ATOM — see spec/ (authoritative) and ATOM_Technical_Blueprint_v4.0.

fn main() {
    println!("atom-cli (ATOM alpha skeleton — G0)");
}
