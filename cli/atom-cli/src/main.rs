//! atom-cli: atom CLI: setup/mission/grant/approval/effect/... (Blueprint §17).
//! ATOM v4 — see spec/ (authoritative) and ATOM_Technical_Blueprint_v4.0.

fn main() {
    println!("atom-cli (ATOM v4 alpha skeleton — G0)");
}
