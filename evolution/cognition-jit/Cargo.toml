[package]
name = "atom-cognition-jit"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ATOM cognition JIT: compile recurrent deliberative behavior to cheaper capability (ATOM-JIT-001, INV-016)"

[dependencies]
atom-evolution = { path = "../../crates/atom-evolution" }
atom-evidence = { path = "../../crates/atom-evidence" }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }

[lints.rust]
unsafe_code = "forbid"
