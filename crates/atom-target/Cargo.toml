[package]
name = "atom-target"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ATOM v4 execution target: typed target enum + mandatory idempotency key with duplicate-dispatch dedup (TGT-001, ATOM-VT-013)"

[dependencies]
atom-effect = { path = "../atom-effect" }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
