[package]
name = "atom-connector"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ATOM connector contract + conformance suite: a connector to an external system is rejected unless it proves digest/idempotency/retry/reconcile/compensate support (CON-001, EFX-002)"

[dependencies]
atom-effect = { path = "../atom-effect" }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
