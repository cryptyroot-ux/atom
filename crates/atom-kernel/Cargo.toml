[package]
name = "atom-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ATOM sovereign kernel: the single unbypassable door for consequential mutation — typed capability authorization AND Effect Kernel commit revalidation, in order (KRN-001)"

[dependencies]
atom-capability = { path = "../atom-capability" }
atom-effect = { path = "../atom-effect" }
chrono = { version = "0.4" }
thiserror = { workspace = true }

[dev-dependencies]
chrono = { version = "0.4" }
