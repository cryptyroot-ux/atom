[package]
name = "atom-ledger"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ATOM append-only hash-chained event ledger with sealed checkpoints (SQLite/WAL)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sha2 = "0.10.9"
hmac = "0.12.1"
hex = "0.4.3"
rusqlite = { version = "0.37.0", features = ["bundled"] }

[dev-dependencies]
tempfile = "3.27.0"

[lints.rust]
unsafe_code = "forbid"
